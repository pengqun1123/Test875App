<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.id_card">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/id_card_ic_launcher"
        android:label="@string/id_card_app_name"
        android:roundIcon="@mipmap/id_card_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/id_card_AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:theme,label">
        <activity android:name=".activity.IDCardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>